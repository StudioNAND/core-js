{
  "name": "@studionand/ada-page-utils",
  "version": "0.0.4",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "prepublishOnly": "echo hallo hallo; npm run build",
    "build": "babel src -d dist",
    "watch": "babel src -d dist --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rielc",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
